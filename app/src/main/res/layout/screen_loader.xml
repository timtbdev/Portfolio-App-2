<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="me.tumur.portfolio.R" alias="R"/>
        <import type="android.view.View"/>
        <variable name="model" type="me.tumur.portfolio.screens.MainViewModel"/>
    </data>

    <!-- Loader Screen -->
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:screenLoader="@{model.screenState}">

        <!-- Icon -->
        <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/loader_screen_icon"
                android:layout_width="@dimen/_80sdp"
                android:layout_height="@dimen/_80sdp"
                app:layout_constraintVertical_chainStyle="packed"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toTopOf="@+id/loader_screen_progress_bar"
                android:contentDescription="@string/cd_loader"
                app:screenState="@{model.screenState}"
                app:loaderIcon="@{R.drawable.ic_loader_avd}"
                tools:src="@drawable/ic_loader_avd"/>

        <!-- Progress Bar -->
        <ProgressBar
                style="@android:style/Widget.Material.ProgressBar.Horizontal"
                android:id="@+id/loader_screen_progress_bar"
                android:layout_width="@dimen/_100sdp"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/loader_screen_icon"
                app:layout_constraintBottom_toBottomOf="parent"
                android:progressTint="@color/progressBar"
                android:indeterminateTint="@color/progressBar"
                android:indeterminate="true"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
